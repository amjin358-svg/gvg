"use client";

/** Layer 1 — subtle drifting perspective grid */
export function AnimatedGrid() {
  return (
    <div className="ai-animated-grid" aria-hidden>
      <div className="ai-animated-grid__plane" />
    </div>
  );
}

export default AnimatedGrid;
